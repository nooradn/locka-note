import React from "react";
import CreateNoteForm from "../content/CreateForm"; 

function AddNoteEH() {
    const newNoteData = {
        id: +Date,
        title: CreateNoteForm.title,
        body: CreateNoteForm.body
    }
}